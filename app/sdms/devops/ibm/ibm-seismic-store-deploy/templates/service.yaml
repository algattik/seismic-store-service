apiVersion: v1
kind: Service
metadata:
  name: {{ include "ibm-seismic-store-deploy.fullname" . }}
  labels:
    {{- include "ibm-seismic-store-deploy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ibm-seismic-store-deploy.selectorLabels" . | nindent 4 }}
